__pycache__/
*.pyc
*.db
*.db-wal
*.db-shm
config.json
# secrets — every per-instance env (host.env, network.env, marsad.env). The
# committed example is marsad.env.example, which is NOT matched by *.env.
*.env
